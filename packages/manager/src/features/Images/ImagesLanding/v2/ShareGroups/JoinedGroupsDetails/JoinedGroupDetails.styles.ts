import { styled } from '@mui/material';

import { CopyTooltip } from 'src/components/CopyTooltip/CopyTooltip';

export const StyledCopyIcon = styled(CopyTooltip)(() => ({
  padding: `0 0 0 8px`,
}));
